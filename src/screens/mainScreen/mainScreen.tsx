import { MainScreenTabNavigator } from '@root/tabs/MainScreen.tsx';
import React from 'react';
import { Text, View } from 'react-native';

export const MainScreen = () => {
  return (
    <View>
      <Text>{'lox'}</Text>
    </View>
    // <MainScreenTabNavigator />
  )
}
